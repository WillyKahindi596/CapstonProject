@model IEnumerable<TasteOfTheWorld.Models.Continent>



<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Continent_Name)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Continent_Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Continent_Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Continent_Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Continent_Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
